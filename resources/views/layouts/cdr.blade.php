<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>@yield('title', 'CDR Teste')</title><link rel="stylesheet" href="{{ asset('css/cdr.css') }}"></head><body>
<header><div class="top"><a class="brand" href="{{ route('reports.index') }}">CDR <strong>Teste</strong></a><nav>
@auth<a href="{{ route('reports.index') }}">Consultas</a>@if(auth()->user()->role === 'admin')<a href="{{ route('admin.dashboard') }}">Painel</a><a href="{{ route('admin.imports.index') }}">Importações</a><a href="{{ route('admin.users.index') }}">Usuários</a>@endif<span>{{ auth()->user()->name }}</span><form method="post" action="{{ route('logout') }}">@csrf<button type="submit">Sair</button></form>@endauth
</nav></div></header><main><div class="content">@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif @if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif @yield('content')</div></main><footer>CDR Teste · Consulta de dados SUP e CADUP</footer></body></html>
